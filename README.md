# LiminalBackrooms

Initial commit - project will contain a Unity URP project for "Liminal Backrooms" (Backrooms exploration / survival game).

This repository will be populated by GitHub Copilot with a ready-to-import Unity project and a Windows build release.

If you (the repository owner) want me to push the assembled Unity project and build artifacts, please ensure the following:

1. The GitHub App or user token used by this assistant has write access to this repository. If you see an error about permission, add write access for the integration or invite the bot account.
2. Optionally, create a branch named `init/unity-project` or allow the assistant to create it. If the repository is empty, the assistant will create an initial commit (this file) and then create the branch.

What I'll do next:
- Create an `init/unity-project` branch.
- Push the assembled Unity project files (Assets/, ProjectSettings/) to that branch.
- Build a Windows standalone and upload it as a Release asset.

If you need any special constraints (private/public release, branch name, Unity version), reply here.
